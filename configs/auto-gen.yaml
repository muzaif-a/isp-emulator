selected:
  - configs/topology_enterprise.yaml       # attacker h2, office LAN, vpnhub
  # - configs/topology_insider_threat.yaml # attacker inside corporate LAN
  # - configs/topology_remote_worker.yaml  # attacker behind home router (remote_access VPN)
  # - configs/topology_three_site.yaml     # 3 corporate sites, site-to-site VPN
  # - configs/topology_dmz_segmented.yaml  # no exfiltration.attacker — exfil auto-skipped
  # - configs/topology_wan_isp.yaml        # no exfiltration.attacker — exfil auto-skipped
  # - configs/topology.yaml

repeat: 1   # runs per combo — different synthetic data each run (random row values)

vpn:
  - on    # WireGuard tunnel active — watermark traverses encrypted VPN
  # - off   # direct path — no VPN overhead

exfil:
  - on    # TOS-marked /backup GET → application-layer watermark → label=1
  # - off   # baseline NPC-only capture, no exfil → label=0
