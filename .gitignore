# ── Claude / IDE ──────────────────────────────────────────────────────────────
.claude/
.agents/
skills-lock.json

# ── Python runtime ────────────────────────────────────────────────────────────
__pycache__/
*.pyc
.pytest_cache/
.venv/

# ── Mininet audit artifacts (delete with: sudo rm -rf audit/) ─────────────────
audit/
audit_namespace.sh

# ── Generated / experiment-specific ──────────────────────────────────────────
run.sh
requirements-lock.txt

# Frozen timing constants — never edit, never commit changes
configs/preregistration.yaml

# ── Generated datasets (keep placeholder .gitkeep) ───────────────────────────
dataset/pcapng/*
!dataset/pcapng/.gitkeep

dataset/csv/*
!dataset/csv/.gitkeep

dataset/tmp/*
!dataset/tmp/.gitkeep

# ── Generated session metadata ────────────────────────────────────────────────
dataset/schema.json
dataset/network_profile.json
dataset/watermark_log.txt
dataset/watermark_log.jsonl

# ── Runtime temp files ────────────────────────────────────────────────────────
/tmp/timing_*.json
/tmp/api_*.log
/tmp/api_*.py
/tmp/watermark_debug.log
/tmp/.app_state_*.db
